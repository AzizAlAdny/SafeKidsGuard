# Safe Kids Guard — FastAPI Backend Dependencies
# Python 3.14 compatible

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Validation & Settings
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0

# Auth & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# AI / NLP
transformers>=4.44.0
torch>=2.3.0

# Notifications
firebase-admin>=6.5.0          # FCM: child app push + web push fallback
httpx>=0.27.0                  # WhatsApp Business API (async HTTP — NOT requests)

# Caching
redis[asyncio]>=5.0.0          # Classification result cache (mandatory)

# Rate limiting
slowapi>=0.1.9

# Reporting
reportlab>=4.2.0
pandas>=2.2.0

# Utilities
beautifulsoup4>=4.12.0         # HTML stripping for Arabic preprocessing
python-dotenv>=1.0.0
python-multipart>=0.0.9        # Form data / file uploads

# Code quality
ruff>=0.4.0
black>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                  # Also used as async test client
factory-boy>=3.3.0
